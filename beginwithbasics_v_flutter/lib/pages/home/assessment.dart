import 'package:flutter/material.dart';

class Assessment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.fromLTRB(100, 0, 0, 0),
      child: Text('Assessment wrks'),
    );
  }
}
